# Gradle 빌드/실행에 사용할 JDK 경로 (이 프로젝트는 Java 17 필요)
# - 시스템 JAVA_HOME과 무관하게 이 JDK로 빌드/실행됨
# - 다른 PC에서는 본인 환경의 JDK 17 경로로 수정할 것 (경로 구분자는 / 사용)
#   (JAVA_HOME이 이미 JDK 17이라면 이 줄을 삭제해도 됨)
# - 상세 안내: docs/jdk-setup.md
org.gradle.java.home=D:/dev/java/jdk-17.0.6
